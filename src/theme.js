import { createTheme } from '@mui/material/styles';
const theme = createTheme({
    palette: {
      primary: {
        main: '#416d3e', // Customize primary color
      },
      secondary: {
        main: '#a6cba4', // Customize secondary color
      },
      third: {
        main: '#edf4ec', // Customize secondary color
      },
      // Add more palette options as needed
    },
    typography: {
      fontFamily: 'Roboto, sans-serif', // Customize typography
    },
    // Add more theme options as needed
  });
  
  export default theme;
